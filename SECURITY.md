# Security

The deterministic R1 prototype requires no production credentials, private keys, or external network access.

Do not insert real credentials or sensitive data into scenarios.

Future higher-fidelity experiments should use synthetic credentials, allowlisted tools, controlled network access, and isolated execution environments.
